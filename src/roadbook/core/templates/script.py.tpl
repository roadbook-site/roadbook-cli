"""
Automation script for roadbook: {name} ({rb_id})
Generated by Roadbook CLI at {date_str}.

[AGENT INSTRUCTION - BEST PRACTICES]
1. Use the RoadbookContext for lifecycle and I/O management. ABSOLUTELY DO NOT REMOVE THIS OR FALLBACK TO RAW PLAYWRIGHT API.
2. Selectors: Prefer semantic locators (e.g., `page.get_by_role()`, `page.get_by_text()`) over brittle XPath/CSS.
3. Variables: DO NOT hardcode search queries or credentials. Extract them to the `rb.get_input()` dict.
4. Artifacts: Save all screenshots, downloads, and extracted data to `rb.storage` or `rb.push_data()`.
5. Sheet Binding: Wrap logic for each phase in `with rb.sheet("Sheet Name"):` to match roadbook.md.
6. CDP Connection: If you need to debug or bypass detection, pass `cdp_url="http://localhost:9222"` into `RoadbookContext(cdp_url=...)` directly, DO NOT put debugging ports in `inputs.json` or `roadbook.md`.
"""
from roadbook.sdk import RoadbookContext

def run():
    # You can pass cdp_url="http://localhost:9222" if you need to attach to an existing browser
    with RoadbookContext() as rb:
        page = rb.page
        
        # Example of getting inputs
        # entry_url = rb.get_input("entry_url", "{entry_url_fallback}")
        
{logic_body}
        
        # rb.push_data({{"status": "success"}})
        rb.logger.info("All sheets completed successfully")

if __name__ == "__main__":
    run()
